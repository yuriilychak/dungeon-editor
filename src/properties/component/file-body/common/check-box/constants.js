const stylePrefix = "check-box-";

export const STYLES = {
    ROOT: `${stylePrefix}wrapper`,
    LABEL: {
        label: `${stylePrefix}label-label`,
        root: `${stylePrefix}label-root`
    },
    CHECK_BOX: {
        root: `${stylePrefix}icon-root`
    }

};
